const observableModule = require("data/observable");

function MyLendingsViewModel() {
    const viewModel = observableModule.fromObject({

    });

    return viewModel;
}

module.exports = MyLendingsViewModel;
